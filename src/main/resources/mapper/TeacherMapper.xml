<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.xmut.examsys.mapper.TeacherMapper">
    <insert id="insertTeacher">
        insert into teacher(tno, username, sex, real_name, email, dept, birth, `desc`, password,
                            id_card, phone)
        values (#{tno}, #{username}, #{sex}, #{realName},
                #{email}, #{dept}, #{birth}, #{desc}, #{password},
                #{idCard}, #{phone});
    </insert>


    <select id="getAll" resultType="edu.xmut.examsys.bean.Teacher">
        select *
        from teacher
    </select>
    <select id="getByTno" resultType="edu.xmut.examsys.bean.Teacher">
        select *
        from teacher
        where tno = #{tno}
    </select>


    <resultMap id="BaseResultMap" type="edu.xmut.examsys.bean.Teacher">
            <id property="tno" column="tno" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="sex" column="sex" jdbcType="TINYINT"/>
            <result property="realName" column="real_name" jdbcType="VARCHAR"/>
            <result property="email" column="email" jdbcType="VARCHAR"/>
            <result property="dept" column="dept" jdbcType="VARCHAR"/>
            <result property="birth" column="birth" jdbcType="TIMESTAMP"/>
            <result property="desc" column="desc" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="idCard" column="id_card" jdbcType="VARCHAR"/>
            <result property="phone" column="phone" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="TINYINT"/>
            <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="isDeleted" column="is_deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        tno,username,sex,
        real_name,email,dept,
        birth,desc,password,
        id_card,phone,status,
        avatar,create_time,update_time,
        is_deleted
    </sql>
</mapper>
